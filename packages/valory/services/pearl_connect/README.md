# Pearl Connect service

Service package for the Pearl Connect BYOA agent. Pearl creates the service
from a template referencing this package's IPFS hash; its connection overrides
define the environment variables the `pearl-connect` binary consumes.