# Connect service

Service package for the Connect BYOA agent, minted on-chain as
`valory/connect`. Pearl creates the service from a template referencing this
package's IPFS hash; its connection overrides define the environment variables
the `connect` binary consumes.